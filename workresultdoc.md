# 功能交付记录（Work Result Doc）

按天记录项目功能的新增/优化/升级，便于回溯与理解项目演进。**每次完成可交付功能并交付给用户后，须在本文件追加一条记录。**

| 日期       | 功能名称             | 功能实现简述                                                                                                                                 | 状态   |
| ---------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 2026-06-22 | 命理平台 UI 重构 Mockup · 设计评审四项整改（/plan-design-review） | 基于 /plan-design-review 评审对 `testUI/` 做四项整改：①**IA 审计**：通读 src/app/(main)/ 全部 13 个页面（/、/chart、/charts、/charts/[id]、/reports、/reports/[id]、/compatibility、/profile、/user、/pricing、/pricing/mock-pay、/auth/login、admin/*）+ Header/MobileNav 组件 + 50+ API 路由，建立权威 IA 清单（含字段、跳转、状态机）。②**缺口补全**：profile.html 加流水统计概览（总获得/总消费/余额三卡）+ 资产快捷三卡（星币/报告/立即充值）+ 兑换码弹窗 + 充值套餐弹窗（4 档：¥20/¥50/¥100/¥300）+ 关系网络图入口 + 系统设置区（5 项菜单：服务权益/兑换中心/偏好设置/隐私协议/关于我们）；charts-list.html 给 3 张命盘卡片全部补「AI 解盘 / 生成报告 / 合盘」三个跳转按钮（对应当前项目 `/charts/[id]?action=chat|report` + `/compatibility?selfChartId=`）；pricing.html 从 3 档（月/季/年）扩为 4 档（加「免费体验」对应当前项目 FREE 档）+ 加模拟支付开发入口链接。③**粘土风浮雕增强**：ziwei.css 中 [data-theme="clay"] 的 --shadow 从 `12px/0.22`+单层 inset 升级为 `16px/0.50`+双向 inset（亮高光 0.70 top-left + 暗投影 0.30 bottom-right）；--shadow-lg 从 `16px/0.18` 升级为 `22px/0.55`+双向 inset 0.80/0.35；圆角变量从 18/12/28px 扩为 20/14/30px；新增 --shadow-inset 用于内嵌组件；themes.css 同步升级（--theme-shadow-card / card-hover / btn / btn-hover / btn-active / btn-primary / input / input-focus / nav-hover / nav-active / icon / badge / bar 共 13 个阴影变量全部强化位移/不透明度/双向 inset），圆角从 28/18/16/12 扩为 30/20/18/14。④**报纸主题圆角化**：批量替换 ziwei.css 中所有 `border-radius: 0;`（共 ~30 处覆盖：auth-card/level-badge/data-table/pattern-card/triad-card/matter-card/progress-step/identity-card/primary-tag/identity-switcher/log-item/chart-toggle/yunlin-bar/home-user-card/home-quick/tool-call/analysis-card/setting-row + 默认样式 btn/card/modal/input/chip/help-note/seg/lock-sec/compat-result/plan-card/share-card/chart-list-item 等共 ~50 处）统一为 `var(--radius-sm)`（报纸 4px / 粘土 14px / 新拟态 10px），保留 help-note 左侧色块 `0 var(--radius-sm) var(--radius-sm) 0` 与 rail active 指示条 `0 3px 3px 0` 的局部方角设计。自测：HTTP 8765 全部资源 200、`border-radius: 0;` 残留 0、粘土浮雕新变量出现 8+ 次、profile.html 5 项补全关键词命中 2-3 次/项。 | 优化 |
| 2026-06-22 | 命理平台 UI 全面重构 Mockup（Chat 模式 + 三主题 + 三端覆盖） | 对标当前 chat 类大模型交互范式，重构 `testUI/` 前端 Mockup：①沿用现有 css/js 资产（ziwei.css 44KB + themes.css + common.js + theme.js），在 ziwei.css 末尾追加 ~400 行补充样式（能级徽章/Tab 切换/数据表格/雷达图/格局卡片/性格三宫/事项卡片/报告生成进度/命主管理/星币流水/命主切换器/H5 tabbar/H5 顶栏/工具调用提示/分析卡片/分组标题/设置项/12 宫方阵完整化等）。②目录按平台分子目录：`desktop/`(17 页) + `h5/`(10 页) + `miniprogram/`(SPEC.md 规范文档) + `shared/`(12 宫方阵 snippet) + `INDEX.html`(三端总入口)。③桌面端核心 6 页：auth-login（密码/验证码/注册+微信）、home-logged-out（产品介绍+CTA）、home-logged-in（欢迎卡+命盘+最近咨询+报告）、chat-new（出生信息完整表单+真太阳时+父母四化）、chat-active（核心交互：消息流+12 宫方阵抽屉+事项chips+会话侧栏+模型切换+工具调用提示+分析卡片）、chart-detail（5 Tab 全维度：方阵/能级/格局/性格三宫/事项分析/AI 性格）。④桌面端辅助 11 页：charts-list（命主分组+CRUD）、reports-list（9 模板+历史）、report-generating（7 阶段进度+脉冲动画）、report-detail（章节+部分锁定+Q&A）、compatibility-select（双选+关系+维度+AI 解读选项）、compatibility-result（综合评分+五维雷达占位+四化交叉+12 宫对照+亮点/风险+AI 深度解读）、profile（用户卡+6 项统计+命主管理+星币流水+邀请码）、settings（个人信息/安全/偏好/通知/隐私/关于六组）、pricing（月/季/年套餐+按次付费+星币 4 档包+权益对比表）、share-landing（8 渠道+分享卡预览+二维码弹窗+奖励规则+历史记录）、index（mockup 评审索引+13 需求覆盖矩阵）。⑤H5 移动端 9 页：home/chat（命盘从底部抽屉滑入）/chart-detail/charts-list/reports（列表+详情合并）/compatibility（选择+结果合并）/profile/pricing/auth-login，全部含底部 5 项 tabbar。⑥小程序设计规范 miniprogram/SPEC.md：组件映射表（div→view/span→text/@click→bindtap）、3 个原生改造点（对话 SSE→WebSocket、支付→wx.requestPayment、分享→button open-type=share）、Taro 框架推荐、API 复用方案（含需新增 3 个小程序专用端点）、UX 差异点、P0/P1/P2 优先级。⑦全程遵守核心约束：后端零改动（不动任何 API/配置/数据库）、默认报纸主题+粘土+新拟态三主题可切换、布局参考 ~/Downloads/index.html 的 72px 细菜单条 + chat 骨架。自测：python3 -m http.server 8765 启动后 34 个资源全部 HTTP 200、桌面 15 页全含 rail+main 结构、H5 8 核心页全含 5 项 tabbar、27 页全引入 theme.js、关键元素齐全（命盘方阵/抽屉/消息流/能级徽章/雷达图/分享逻辑/进度步骤等）、三主题规则 53+7+7 条齐全、settings.html 修复一处 typo。 | 新增 |
| 2026-06-18 | 命理平台静态 UI 功能补全（对标真实项目非后台功能） | 对照真实 Next.js 项目非管理后台功能，补齐 `testUI/` 缺失模块：①新增 `login.html`（账号/手机/微信登录与注册）。②新增 `user.html`（命主 Identity 档案管理 + 星币流水 + 兑换码 + 快速充值）。③新增 `charts.html`（命盘列表，含来源标签/主星摘要/AI 解盘/生成报告入口）。④新增 `chart-detail.html`（单命盘详情 + 十二宫渲染 + 跳转解盘/报告/合盘）。⑤新增 `report-detail.html`（报告内容 + 子报告进度 + 报告问答）。⑥新增 `promoter.html`（推广中心：收益/团队/收益明细/邀请链接）。⑦新增 `mock-pay.html`（模拟支付页，会员与星币充值均走此页）。⑧扩展 `js/common.js` 支持用户、命主、星币流水、咨询会话、推广数据持久化。⑨扩展 `css/ziwei.css` 增加 switch 开关、认证页、阅读页模型切换/会话列表、用户中心、报告详情等样式。⑩更新 `index.html`：排盘表单增加真太阳时开关与父母出生年份；首页功能卡片增加「情绪疏导」；AI 解盘页增加模型切换器（MiniMax/DeepSeek/智谱/通义千问/Claude）与咨询会话侧边栏。⑪更新 `profile.html`/`reports.html`/`pricing.html`/`share.html` 链接到新页面并补充推广中心、命盘列表、报告分类、模拟支付等入口。验证：全部 13 个静态文件 HTTP 200，本地引用有效。 | 新增 |
| 2026-06-18 | 命理平台静态 UI 逻辑与主题优化 | 在首轮 UI 重设计基础上，按项目实际产品逻辑重新调整交互流程与页面结构：①`index.html` 改为「首页 → 排盘 → 命盘结果 → AI 解盘」清晰主线，用步骤条引导；增加当前命盘右侧面板全局可调出。②新增共享 `js/common.js`（星币/命盘 localStorage 持久化）与 `js/theme.js`（三主题切换），统一 6 个页面主题状态与数据。③`profile.html` 明确「命主命盘（唯一）+ 其它命盘」两层管理，支持设为主、使用、删除。④`reports.html` 改为「报告中心 → 选择命主 → 选择模板 → 生成报告」两步流程，含历史记录。⑤`compatibility.html` 支持从已有命盘中选择双方并预览主星/五行。⑥`pricing.html` 区分会员套餐与星币充值。⑦`share.html` 增加邀请码/链接复制与分享记录。⑧`css/ziwei.css` 统一为报纸主题（默认）、粘土主题、新拟态主题三套主题变量，所有组件使用变量，右下角主题切换器全局可用。验证：全部 6 页面 HTTP 200、本地引用有效、外部/内联 JS 语法通过 `node --check`、旧 CSS 文件保留未引用。 | 优化 |
| 2026-06-18 | 命理平台静态 UI 重设计 | 参照 `/Users/martin/Downloads/index.html` 的左侧 Rail + 主内容区 + 右侧滑出命盘面板的交互框架，在 `testUI/` 下重新设计静态模拟站点。新增 `testUI/css/ziwei.css` 共享样式（现代东方/深色科技/传统命理三套主题），新增 6 个页面：`index.html`（首页/AI 解盘/排盘 SPA 三视图，含出生信息弹窗、紫微 12 宫方阵、报告弹窗、会员弹窗）、`profile.html`（个人中心/命主命盘/多命盘管理）、`reports.html`（报告中心/生成报告/历史记录）、`compatibility.html`（双人合盘/关系分析）、`pricing.html`（星币充值/月卡季卡年卡会员）、`share.html`（微信/微博/链接/二维码分享赚币）。实现星币消耗提示、命盘展开/收缩、保存与调出模拟、主题切换。本地 `python3 -m http.server 8888` 启动验证，全部页面 HTTP 200，HTML 与内嵌 JS 语法检查通过。 | 新增 |
| 2026-06-18 | 命盘保存 + Stage1-2 持久化复用机制 | 解决同一命盘被反复重算 Stage1-2（宫位评分/四化/格局/大限流年/锚定宫位/性格定性）拖慢 AI 对话与报告生成的问题。调查发现 80% 基础设施已存在（ChartRecord 表 + ChartSnapshot 含完整 stage1/2 + saveChart + getChartSnapshot + SaveChartButton + /charts + /reports），真正缺的是 5 个"接线点"和 orchestrator 的 DB 持久缓存层。改造点：①`chart-snapshot-builder.ts` 新增 `STAGE_VERSION='v1'` 版本治理（规则升级时 bump，旧 snapshot 自动失效重算）+ `buildChartSnapshotFromReading`（复用前端 chartData 跳过排盘，与原 buildChartSnapshot 输出 deep equal）+ `chartDataToBirthInfo`（orchestrator 查 DB 的指纹基础）+ `TIME_INDEX_TO_HOUR`；ChartSnapshot.stage1/stage2 类型从 `unknown` 收紧为 `Stage1Output/Stage2Output`；提取 `assembleSnapshot` 共用组装逻辑。②`chart-record-service.ts` 的 `saveChart` 支持 chartData 路径 + 版本过期自动重算回写 + 新增 `findSnapshotByFingerprint`（带兼容性校验）。③`orchestrator.ts` Redis miss 后插入 DB 持久缓存查询层（跨 session/跨 24h 复用 stage1/2，DB 故障降级不阻塞主流程，回写 Redis 补热）。④`SaveChartButton` + `save-from-session` API 传 chartData（避免服务端重排导致与用户所见数据漂移）。⑤`chart/page.tsx` 的 `?chartRecordId` 加载改用 `snapshot.reading` 作为 chartDataForPipeline（Iztrolabe 实例仍走 astro.bySolar 重建，reading/stage1/2 走快照）。⑥`reports` 页 + `/api/reports` 强制 `chartRecordId` 必填（前端加命盘选择器+命主联动加载命盘列表，无保存命盘时友好提示去排盘）。验证：tsc 零新增错误（4 个预存错误在 untracked 的 daxian-verification.test.ts WIP 调试脚本，非本次引起）、12 新单测+91 现有测试全过、dev 重启编译首页 200、4 个 API 路由冒烟全活。 | 升级 |
| 2026-06-17 | 报告生成复用排盘页 stage3-4 链路 | 解决报告生成与排盘页面 AI 解读口径不一致问题。改造点：1) `orchestrator.ts` 导出 `buildStage3Messages` 纯函数；2) `prompt-builder.ts` 导出 `getSystemPrompt`；3) 新建 `report-governor-runner.ts`：对每个 matterType 并行调用 stage3 governor（与排盘页同源），失败降级到 stage3.analysisSummary；4) 重写 `report-generator.ts`：删自写 SYSTEM_PROMPT，改用 5 段 messages（getSystemPrompt + IR+dataPanel + STAGE2_HINT + 模板特殊指令 + governor 解析结果）；5) `report-pipeline.ts` 改为返回 `{ ir, stage1, stage2, stage3List, chartData, targetYear }` bundle，让 governor 复用 stage 中间值；6) `reports/route.ts` 主报告与子报告链路均插入 governor 预解析；7) 模板特殊指令迁移到 `ReportTemplate.promptConfig` DB 字段，新建 `scripts/seed-template-prompt-config.ts` 已 seed 9 个主模板；8) 提取 `template-instructions.ts` 作为单一数据源。**修复 past-life 模板 dataPanel 缺四化/大限数据 bug（fallback 到 stage1.allDaXianSummary / mergedSihua.palaceAnnotations）；修复 TEMPLATE_MAP 的 `compatibility` → `compatibility-report` slug 不匹配 bug**。全面验证：①9 模板 IR/prompt 静态检查 570/570 全通过（含 stage1-2 全字段、stage3 拼装、模板主题关键词 100% 命中）；②talent-awakening 端到端：governor 2225 字、主调用 5 章 5949 字；③life-full-analysis 端到端：4 matters 并行 governor 共 11608 字、主调用 10 章 6281 字完全对齐模板指令。 | 升级 |
| 2026-06-05 | 事项分析数据格式规范修复 | 1) 核心修复：orchestrator 中将 `buildMatterAnalysisData()`（旧路径，命宫硬编码、缺天干地支、primary 非数组、引动为空）替换为 `formatMatterAnalysis()`（新路径，严格按《通用单人事项分析数据格式规范》实现）；2) IR 数据注入增强：规范 JSON 作为知识片段注入到 #2 区块，包含完整三层十二宫（原局/大限/流年）数据；3) 知识注入器增强：`injectStage3Knowledge()` 新增大限十二宫摘要、大限格局列表、大限两夹宫信息；4) 调试面板更新：HybridDebugPanel 新增 #2 IR 数据注入（JSON）区块和 #5 用户问题（组装 Prompt）专用区块；5) debugInfo 类型新增 `irDataJson` 字段；6) `KnowledgeSnippet.source` 新增 `'大限数据'` 枚举值。 | 修复 |
| 2026-06-04 | UI 设计规范统一 | 报告页、用户中心页、报告详情页全面改造，统一为首页/定价页的设计标准：1) 主色调从 orange/zinc 改为 primary（深紫）+ semantic token；2) 背景从深色(zinc-950/800)改为浅色(primary/5渐变+background)；3) 卡片统一 border-primary/10 + hover:shadow-primary/5；4) 标题加 font-[var(--font-serif-sc)] 衬线字体；5) 按钮/徽章/进度条全部改为 primary token；6) 布局宽度从 max-w-2xl 统一为 max-w-4xl/6xl；7) 去除所有硬编码 orange/zinc/white，改用语义色。 | 优化 |
| 2026-06-03 | 报告系统 + 用户中心 + 命主管理 | 参照竞品"寻一"全量复制核心功能：1) Prisma Schema 新增 8 个模型（Identity/ReportTemplate/Report/ReportQuestion/RedeemCode/RedeemUsage/PromoterProfile/PromoterTeam/PromoterEarning）；2) 14 个 API 路由（报告模板、报告CRUD+状态轮询+问答、命主CRUD+激活、兑换码、推广系统、充值）；3) /reports 报告页（Hero Banner + 双Tab + 9种模板卡片 + 我的报告列表 + 生成弹窗 + 自动刷新）；4) /user 用户中心页（深色主题资料卡 + 命主档案管理 + 余额卡片 + 会员升级横幅 + 设置菜单）；5) 种子数据脚本（9个报告模板 + 测试兑换码）。 | 新增 |
| 2026-05-25 | 父母出生年份输入功能 | 1) 新建 gan-zhi.ts 纯工具函数（yearToGan/yearToZhi/yearToZodiac）；2) iztro-reader 接受 parentBirthYears，注入父母干支到 ScoringContext，激活 step 2.4-2.7/4.4-4.7 父母四化加减分；3) 前端表单：年份+生肖拆分选择器，用户选年后点选正确生肖（春节前后跨两生肖年），根据选中生肖自动修正农历年份；4) 宫位能级详情展示父母四化具体干支和星曜名（如「父亲己干化禄·武曲」）；5) chart-pipeline-debug 新增 parentSihuaMeta 元数据；6) IR/Prompt 展示父母生年干支信息；7) ReadingRequestSchema 加 .passthrough() 保留 rawDates，修复太岁宫位错误；8) sessionStorage 持久化父母生肖选择。 | 新增 |
| 2026-05-24 | AI prompt 太岁宫位修复 | ReadingRequestSchema（Zod）只白名单 palaces/birthInfo/horoscope，strip 了 rawDates/chineseDate/solarDate，导致 iztro-reader 无法从农历数据覆盖生年干支，退回阳历直接算（2000→庚辰，实际己卯）。给 chartData 的 Zod object 加 .passthrough() 修复。 | 修复 |
| 2026-05-24 | 格局判定系统性 bug 修复 | 修复三大格局判定 bug：1) 四化被当星名处理导致误匹配；2) AND/OR 条件语义错误（要求所有子条件同宫才算满足）；3) 吉煞引动条件过于宽松。修正后格局识别准确率显著提升。 | 修复 |
| 2026-05-24 | 宫位能级展示修复 + API 清理 | 1) 宫位能级详情页展示修复，对齐评分引擎实际输出；2) 清理遗留 API 路由和冗余模块；3) 删除 pattern_library.json 冗余 definitions 段。 | 修复 |
| 2026-05-20 | config 层迁移入 src/core | 将 config 配置层从根目录迁移入 src/core，增强性格分析管线，清理遗留调试脚本，统一项目目录结构。 | 重构 |
| 2026-05-19 | 清理遗留 RAG/分析模块 | 清理遗留的 RAG/skills/hybrid/zvec/analysis 模块及关联管理页面，精简代码库，减少维护负担。 | 重构 |
| 2026-05-15 | 流式输出 JSON 过滤与 AI 超时 | 1) 流式输出过滤尾部 JSON 块（intent/memory_update），避免 AI 回复中显示原始数据；2) 延迟缓冲方案可靠过滤；3) AI 请求超时从 30s 提升到 120s，避免 Hybrid 长回复中断。 | 修复 |
| 2026-05-14 | Hybrid RAG 管道 + 分析面板重构 | 1) Hybrid RAG 管道实现（知识库检索 + AI 生成）；2) AI 模型管理后台增强；3) 分析面板重构（统一分析入口）；4) 主题系统支持。 | 新增 |
| 2026-05-14 | 评分/路由配置数据驱动化 | 1) 评分权重和路由规则从硬编码改为数据驱动（配置文件/数据库）；2) 会话管理增强（历史记录、上下文保持）。 | 重构 |
| 2026-05-14 | 构建问题修复 | 1) 添加 lunar-lite 依赖和 .npmrc 配置；2) 修复 TS 严格模式类型错误；3) pnpm 提升配置修复；4) 修复 rebase 导致的缩进错乱。 | 修复 |
| 2026-05-04 | 情感分析功能 | 1) 情感分析功能开发（文本情感检测与分类）；2) 多 AI Provider 优化（统一接口、错误处理、降级策略）。 | 新增 |
| 2026-05-02 | ICP 备案信息 | 添加 ICP 备案信息到页面底部，满足合规要求。 | 新增 |
| 2026-05-01 | step4 优化 + 产品改名 | 1) 参照 v2 优化解盘 SYSTEM_PROMPT 和 assembleContext，提升 AI 回复质量；2) 产品从"紫微心理"改名为"微著"，"紫微命理排盘"改为"微著排盘"。 | 优化 |
| 2026-04-30 | 方案C 部署脚本与生产部署 | 1) 方案C 部署脚本（pnpm + 服务器构建）；2) 修复 pnpm 在腾讯云无法安装问题（改用 npm 优先）；3) 改用 nohup 直接启动替代 PM2；4) 完善部署指南文档。 | 部署 |
| 2026-04-29 | RAG 意图识别 + ChatPanel UX 优化 | 1) RAG 意图识别功能（分析类型自动分类）；2) ChatPanel UX 优化；3) 排盘默认时间修复；4) 移除非流式自动聚焦；5) suppressHydrationWarning 修复。 | 新增 |
| 2026-04-28 | 生产部署工具链 + RAG 管道增强 | 1) 生产部署工具链完善；2) RAG 管道增加年份/运限数据支持；3) 调试面板增强；4) imemory 教训补录。 | 优化 |
| 2026-04-27 | 四步 RAG 精准召回管道 | 1) 四步 RAG 精准召回管道实现（查询→检索→重排→生成）；2) ChatPanel 接入 RAG；3) 向量化修复；4) Bug 修复。 | 新增 |
| 2026-04-25 | 首页性能优化 | 1) JWT 回调优化：仅登录时查库，后续从 token 读取，消除每次页面加载的 DB 查询；2) 字体子集化：4.4MB→1.3MB（覆盖 3900+ 字符），减少 70% 带宽；3) Suspense 包裹 Header/MobileNav + 骨架屏；4) 新建 (main) 路由组 loading.tsx | 优化 |
| 2026-04-25 | 上下文优化与 Skills 管理 | 1) claude-mem 注入量 50→15 条观察、会话数 10→3；2) 移除 23 个不相关 skill 到 _disabled 备份；3) 项目/全局 CLAUDE.md 精简；4) 新安装 prisma/context7/feature-dev/security-guidance 插件，重新启用 product-analysis/competitors-analysis/prompt-optimizer/github-ops/scrapling-skill | 优化 |
| 2026-04-24 | 知识库目录迁移与四库架构 | 1) 知识库数据目录从 `logicdoc/` 迁移至 `sysfiles/sysknowledge/`，标签库从 `systag/` 迁移至 `sysfiles/systag/`；2) 建立四库架构：systag（标签库）、sysknowledge（知识库）、sysrules（规则库）、systech（技法库），外加 sysmapping（映射表）；3) 更新所有代码中的数据路径引用（20+文件）、向量库路径（logicdoc_dim* → sysknowledge_dim*）、索引版本（sysknowledge-v3）；4) 更新文档（CLAUDE.md、CONTEXT.md、文件对照表）。 | 重构   |
| 2026-04-24 | 移除硬编码管理员密码 | 移除 `auth/index.ts` 中的 `isBuiltInAdmin` 硬编码后门（37行），改为标准数据库密码校验；`prisma/seed.ts` 管理员密码改为从环境变量 `ADMIN_PASSWORD` 读取并写入 `AdminConfig` 表；全项目 `ffffff` 零残留。 | 安全修复 |
| 2026-04-24 | 项目 GitHub 上传准备 | 1) .gitignore 排除敏感文件（内部文档、AI配置、sysfiles/、data/、node_modules/等）；2) 创建 GitHub 私有仓库（后改为公开） tomcatmartion/navicauseffect；3) 初始提交 401 文件推送成功。 | 部署   |
| 2026-04-19 | 三层混合打标系统 | 实现知识库 chunk 的三层混合打标：1) tag-rules.json 定义 10 个业务标签体系；2) 管理后台可手动指定文件标签（覆盖规则）；3) AI 批量打标（MiniMax，每批 15 段，返回 1-3 标签，失败回退关键词匹配）；新增标签编辑弹窗、重新打标按钮（不重算向量）；向量化时自动集成 AI 打标。测试命中率 100%。 | 新增   |
| 2026-04-19 | 管理后台知识库管理 + RAG 检索测试 | 新增知识库管理页（上传 md/docx/pdf/xlsx、文件列表、向量化进度展示、分段详情查看）；新增 RAG 检索测试页（检索词生成、RAG 结果展示、System Prompt 预览、完整 Messages 展示）；扩展文件解析器支持 PDF/Excel；重构 logicdoc-indexer 支持新格式+进度跟踪；提取 chart-context.ts 共享模块。 | 新增   |
| 2026-04-19 | Chat API 流年/小限数据准确性修复 | 修复 chat API 中流年/小限数据标注错误（当前年数据被标为目标年）和一般问题缺失流年/小限数据的问题；提取 buildChartContext 为共享模块消除重复代码；修复 currentYear 硬编码 2026。 | 修复   |
| 2026-04-08 | logicdoc 单维索引 + RAG 自测 | 修复仅配 1536（如 MiniMax）时索引脚本因缺 1024 配置整次失败、Zvec 无向量导致全模块「无匹配片段」：`runLogicdocZvecIndex` 按已配置维度分别写入并跳过未配维；无结果提示补充单维索引说明；`npm run test:rag-categories` 对 7 个 `AnalysisCategory` 校验 `retrieveLogicdocForAnalysis`。 | 修复   |
| 2026-04-08 | Embedding 解析与后台可维护性 | 修复「HTTP 200 但无向量」：请求默认带 `encoding_format=float`、解析多形态 JSON（含 `output.embeddings`、base64 float32）、Base URL 自动避免重复 `/embeddings`；检索/建库校验向量维与 Zvec 一致；`POST /api/admin/embedding-config/test` + 后台「测试连接」；超时与 `EMBEDDING_OMIT_ENCODING_FORMAT` 可配。 | 优化   |
| 2026-04-08 | MiniMax 式 `vectors` / `base_resp` | 解析 `vectors` 二维数组（及元素内 `embedding`/`vector`）、顶层 `vector`；`base_resp.status_code !== 0` 时抛出业务错误；Embedding 页文案说明。 | 修复   |
| 2026-04-08 | MiniMax 请求体 `texts`+`type` | 检测 minimax 域名时 POST `{ model, texts:[], type: db|query }`；索引用 db、RAG/测试用 query；`EMBEDDING_FORCE_OPENAI_EMBEDDING_BODY` 可强制 OpenAI `input`。 | 修复   |
| 2026-04-08 | Embedding 错误与自测闭环 | `embedding-errors` 增强 base_resp/OpenAI 报错（含 insufficient balance 计费说明）；空输入拦截；HTTP 非 2xx 先解析 `base_resp`；可选 `EMBEDDING_MINIMAX_GROUP_ID`→`group_id`；`isMinimaxEmbeddingBaseUrl` 避免 eslint hook 误报；`npm run test:embedding-pipeline`；后台计费提示与多行错误展示。 | 优化   |
| 2026-04-08 | Embedding 后台 Group ID | `AdminConfig` JSON 存 `groupId`；GET/PUT 与 `readPrev` 合并；MiniMax 请求体 `group_id` 优先用后台、其次 `EMBEDDING_MINIMAX_GROUP_ID` / `EMBEDDING_1536_GROUP_ID`；Embedding 页两维度输入框；`invalid params` 文案指向 Group ID。 | 新增   |
| 2026-03-28 | Embedding PUT 部分保存与字段合并 | 原 `parseDim` 要求单次提交两套均含 baseUrl+modelId，仅填 1536 或漏填 Model ID 时整包 400、库不入账。改为空字段与库旧值合并，按维度独立 upsert（整套齐全才写），可只存一套；页头说明与 400 文案同步。 | 修复   |
| 2026-03-28 | Embedding 后台配置持久化与回读 | 数据存 MySQL `admin_config`（`embedding_config_dim1536` / `dim1024`）。修复 PUT 合并旧 API Key 时误用 `typeof configValue === 'object'`（Json 以字符串返回时读不到旧 Key）；`getEmbeddingConfigStoredShape` 识别 `JsonNull`；PUT 事务写入后回传 `dim1536`/`dim1024` 快照；前端 GET 加时间戳防缓存、保存后用响应更新 state 且静默二次拉取。 | 修复   |
| 2026-03-28 | logicdoc Zvec 双索引 + RAG 解盘 | 两套 Zvec（1536/1024）+ AdminConfig embedding 与 `/admin/embedding`；分块硬提取 stars/palaces/庙旺利陷/时限 + `content_hash` 增量；文件锁单线程索引；`/api/analysis` 按 provider 选维检索，无索引 503；`npm run logicdoc:index-zvec` 与后台重建接口。 | 新增   |
| 2026-03-28 | RAG 与模型映射评审补强 | Provider→1536/1024 显式清单（含 doubao/ark/volcengine）；检索 query 增加运限结构化摘要、`body.userQuestion`；registry 增加「运势」标签与 FORTUNE 过滤；分块注释对齐递归分割策略 + `LOGICDOC_CHUNK_OVERLAP_RATIO`；后台模型增加 OpenAI/Google/豆包并用 OpenAI 兼容 chat。 | 优化   |
| 2026-04-02 | 命盘页左右分栏 + AI 对话功能 | 命盘页改为 PC 左右分栏（左命盘 45%/右 AI 55%），移动端上下堆叠；新增 AI 多轮对话功能（ChatPanel + /api/analysis/chat），SSE 流式输出，前端 State 管理上下文；TypeScript + Build 零错误。 | 新增   |
| 2026-04-02 | Google Fonts 替换为本地字体 | 下载 Noto Serif SC + Noto Sans SC woff2 到 public/fonts/，layout.tsx 改用 next/font/local，构建不再依赖外网。 | 优化   |
| 2026-04-02 | 全面代码审查与文档体系重建 | 审查全部核心代码（API 路由、组件、lib、中间件、Prisma schema）；TypeScript 编译零错误；发现构建因 Google Fonts 网络问题失败（非代码问题）；重建 workresultdoc/CONTEXT/CLAUDE.md/imemory.md/claude.me 五文档体系。 | 优化   |
| 2026-03-28 | MiniMax 提供商（仅后台配置） | 新增 `minimax` Provider（OpenAI 兼容 `https://api.minimaxi.com/v1`）；`/admin/models` 选厂商并填 Key；移除 seed/.env 注入 MiniMax。 | 新增   |
| 2026-03-28 | Prompt 后台与前台浮窗 | AdminConfig `ai_prompts` 可编辑 system + 7 类模块文案（`/admin/prompts`）；解盘接口合并默认；流式首帧 `naviMeta` + 缓存 JSON `promptMessages`；命盘页弹窗展示本次完整 prompt。 | 新增   |
| 2026-03-28 | 登录后 session 被清空修复 | JWT callback 在 Edge（middleware）中不再调用 Prisma；避免解码失败清空 cookie、登录成功却无法进 `/admin`/`/profile`。`npm run test:auth` 已通过。 | 修复   |
| 2026-03-28 | AI 解盘全文输出      | 取消流式/存档的会员预览截断，SSE 全量转发；`previewContent` 与全文一致；分类 prompt 去掉 800–1200 字上限、改为充分展开；默认 `ANALYSIS_MAX_OUTPUT_TOKENS` 提至 16384。 | 优化   |
| 2026-03-25 | 紫微斗数规则解析引擎 | 本地 TypeScript 实现的 ZiweiEngine（格局识别、宫位能级、性格分析、互动关系、事项分析、完整解盘）；`/api/ziwei/analyze` 路由；命盘页新增「规则解析」Tab。 | 新增   |
| 2025-03-07 | workresultdoc 格式   | 表格按日期倒序排列，分隔线与列宽统一，字段说明保留；补全本会话及历史可溯源功能记录。                                                          | 优化   |
| 2025-03-07 | 规则与文档体系       | 新增 workresultdoc.md、CLAUDE.md 工作规范与三文档配合说明、.cursor/rules 交付与复盘规则。                                                     | 新增   |
| 2025-03-07 | 管理功能与权限控制   | 前端 8 个模块（第 8 个「见真连线」弹窗「咨询师下班了」）；后台收费模块管理 /admin/modules；VIP 名单从 AdminConfig 读取。                        | 新增   |
| 2025-03-07 | VIP 专项与付费提示   | 后台可配置 VIP 名单；未付费用户点击 VIP 模块 403，提示升级或单次付费；单次付费用 bonusQueries 扣减。                                           | 优化   |
| 2025-03-07 | 命盘解析存档         | ConsultationRecord 增加 chartFingerprint；同一用户同一命盘同一模块先查存档，命中直接返回不调 AI；新解析结果写入 DB。                           | 新增   |
| 2025-03-05 | 方案B 双 Fork 集成   | 清空既有 iztro 依赖，克隆 iztro + react-iztro 至 packages/，根项目 file: + overrides 单实例；chart 页接回 Iztrolabe，SSR/React19 兼容。          | 新增   |
| 2025-03-04 | Alpha 首版实现       | 命理排盘与命盘展示（iztro 接入）、AI 智能解析 7 模块与限流、会员体系与定价、微信/支付宝支付与回调、手机/微信/邮箱登录、推广分享与积分、管理后台（模型/用户/短信/支付/价格/统计）。 | 新增   |

---

## 字段说明

- **日期**：YYYY-mm-dd，以交付日为准。
- **功能名称**：简短功能标题。
- **功能实现简述**：一两句话概括实现要点（不替代详细设计文档）。
- **状态**：`新增` / `优化` / `升级` / `修复`。

## 更新约定

在每完成一个可交付功能（新增/优化/升级/修复）并交付给用户后，**在同一轮对话中**向本文件追加一行表格记录；若用户明确要求「本次不记」则可不追加。
